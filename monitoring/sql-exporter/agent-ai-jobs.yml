global:
  scrape_timeout: 10s
  min_interval: 30s
  max_connections: 2
  max_idle_connections: 2

target:
  name: agent_db
  data_source_name: 'postgresql://monitor:monitor@agent-db:5432/agent?sslmode=disable'
  collectors: [agent_ai_jobs]

collector_files:
  - "*.collector.yml"
